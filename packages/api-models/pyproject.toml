[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "meridian-api-models"
version = "0.1.0"
description = "REST API for listing models available across configured Meridian providers"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
    "fastapi>=0.110",
    "opentelemetry-api>=1.24",
    "meridian-sdk-provider",
]

[tool.uv.sources]
meridian-sdk-provider = { workspace = true }
meridian-core-errors = { workspace = true }

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "pytest-asyncio>=0.23",
    "httpx>=0.27",
    "opentelemetry-sdk>=1.24",
    "meridian-core-errors",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["src"]
