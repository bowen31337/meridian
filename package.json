{
  "name": "meridian",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "biome check .",
    "format": "biome check --write .",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "openapi-typescript": "^7.0.0"
  },
  "engines": {
    "pnpm": ">=10"
  }
}
